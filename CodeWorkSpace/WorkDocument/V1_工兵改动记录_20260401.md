# 工兵改动记录 V1

## 日期：2026-04-01

## 改动内容

### 1. 新增文件

**CodeWorkSpace/改造后VNA_eVNA_aVNA/benchmark_methods.py**

适配系统输入格式的基准方法测试代码，包含：

- `DataLoader` 类：加载netCDF(CMAQ)和CSV(Monitor)数据
- `FusionBenchmark` 类：执行十折交叉验证
- 支持方法：CMAQ原始数据、VNA、eVNA、aVNA
- 输出：每折R²、MAE、RMSE + 汇总表

## 待完善项

1. **CMAQ站点插值**：当前`extract_cmaq_at_sites`使用最近邻，精度不足，需改用双线性插值
2. **y_model_train**：目前用模拟值，需要真实CMAQ在监测站点的值
3. **十折验证逻辑**：目前是在网格点评估，应该在监测站点评估

## 依赖

- netCDF4
- pandas
- numpy
- scikit-learn

## 使用方法

```bash
cd E:/CodeProject/ClaudeRoom/Data_Fusion_AutoResearch
python CodeWorkSpace/改造后VNA_eVNA_aVNA/benchmark_methods.py
```
